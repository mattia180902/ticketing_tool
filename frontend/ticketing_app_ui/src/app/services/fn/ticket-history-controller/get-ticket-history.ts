/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TicketHistoryResponse } from '../../models/ticket-history-response';

export interface GetTicketHistory$Params {
  ticketId: number;
}

export function getTicketHistory(http: HttpClient, rootUrl: string, params: GetTicketHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TicketHistoryResponse>>> {
  const rb = new RequestBuilder(rootUrl, getTicketHistory.PATH, 'get');
  if (params) {
    rb.path('ticketId', params.ticketId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TicketHistoryResponse>>;
    })
  );
}

getTicketHistory.PATH = '/api/v1/ticket-history/{ticketId}';
